<ul>
    <% @lists.each do |movie|%>
        <li><%= movie.name %> <%= link_to "Delete", list_path(movie), data: {turbo_method: :delete, turbo_confirm: "Are you sure habibi?"}  %> </li>
    <% end %>

</ul>